#include <stdio.h>

int main() {
    int array[5];
    int *ptr = array;

    for (int i = 0; i < 5; i++) {
        scanf("%d", ptr + i);
    }

    for (int i = 0; i < 5; i++) {
        printf("%d ", 2 * *(ptr + i));
    }

    return 0;
}
